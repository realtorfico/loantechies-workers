{
  "name": "softician-api-keepalive",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare cron Worker that pings softician-api /api/health every 5 min to keep the Azure Functions host warm.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "login": "wrangler login"
  },
  "devDependencies": {
    "wrangler": "^4.104.0"
  }
}
